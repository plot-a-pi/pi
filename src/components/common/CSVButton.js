import React from 'react';
import CSV from './CSV';

const CSVButton = ({ children }) => {

  return (
    <>
      <button> Download Icon
        {children}
      </button>
    </>
  );

};

export default CSVButton;
